package com.example.scrolltrek.ui.sharecard

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

object ShareCardStyles {
    val cardShape = RoundedCornerShape(32.dp)
    val qrShape = RoundedCornerShape(12.dp)
    val cardWidth = 320.dp
    val cardHeight = 480.dp
    val badgeShape = RoundedCornerShape(16.dp)

    val textColor = Color.White
    val subTextColor = Color.White.copy(alpha = 0.7f)
}
